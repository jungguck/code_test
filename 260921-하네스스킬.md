# 260921 — 하네스 / 스킬 안내서

> **여기서부터 읽으면 된다.** 내일 다른 컴퓨터에서 QWEN에게 시킬 때 필요한 것이 전부 여기 있다.

---

## 0. 한 줄 요약

**백준 정답 코드 74개**에 "이 문제가 뭘 묻는지 + 이 코드가 왜 이렇게 푸는지" 설명을 붙여서
**카드 74장**을 만들고, 그걸 텔레그램 봇이 매일 **3분류에서 한 장씩** 보내준다.

---

## 1. 읽는 순서

| 순서 | 파일 | 왜 읽나 |
|---|---|---|
| 1 | **이 문서** | 전체 그림 |
| 2 | `study/cards/03_dp/2579.md` | **완성된 카드 샘플.** 만들 물건이 뭔지 눈으로 봐야 한다 |
| 3 | `study/cards/02_ds/9012.md` | 샘플 두 번째 |
| 4 | `study/cards/PROMPT.md` | **QWEN에게 복붙할 프롬프트.** 카드는 이걸로 찍어낸다 |
| 5 | 4장 아래 "텔레그램 봇 설계" | 봇을 만들 때 |

바쁘면 **2번 → 4번**만 봐도 카드 만들기는 시작할 수 있다.

---

## 2. 지금 상태

```
study/
├── ref/            정답 코드 74개  (사람들이 공유한 것. 검증됨. 고치지 말 것)
├── problems.json   문제 번호 · 제목 · 난이도 · 분류 (186개)
├── cards/          ★ 여기가 결과물
│   ├── PROMPT.md       QWEN에게 줄 프롬프트
│   ├── build.py        카드 검사 + cards.json 생성
│   ├── cards.json      봇이 읽는 파일 (자동 생성)
│   ├── 01_graph/       그래프탐색·완전탐색   0 / 33 장
│   ├── 02_ds/          자료구조             1 / 23 장   (9012 완성)
│   └── 03_dp/          DP                  1 / 18 장   (2579 완성)
└── bank/           (참고) 직접 만든 자작 문제 9개. 이번 일과는 별개
```

**74장 중 2장 완성. 72장 남음.**

남은 목록은 이 명령으로 본다.
```
python study/cards/build.py --todo
```

---

## 3. 카드 만들기 — QWEN에게 시키는 법

### 순서
1. `python study/cards/build.py --todo` 로 안 만든 문제 하나를 고른다
2. `study/ref/{번호}.py` 를 연다 (정답 코드)
3. `study/cards/PROMPT.md` 의 `=====` 안쪽을 복사해서 QWEN에게 준다
   — `{번호}` `{제목}` `{난이도}` `{분류}` `{코드}` 를 채워서
4. 결과를 `study/cards/{분류}/{번호}.md` 로 저장
5. `python study/cards/build.py` 로 검사. `[X]` 없으면 통과

### 쉬운 것부터 가라
난이도 순서로 올라가는 게 읽기에도 맞다.
```
02_ds (자료구조, 23장)  ←  Silver IV 가 많아 제일 만만하다
03_dp (DP, 18장)        ←  Silver III 부터. 점화식 개념이 필요
01_graph (33장)         ←  Gold 가 많이 섞여 있다. 마지막
```

### 품질이 핵심이다
카드가 대충 만들어지면 **이 프로젝트 전체가 의미 없다.** 보고 외울 물건이니까.
QWEN 결과가 얕으면 (풀이 아이디어가 서너 줄이거나, 코드를 한국어로 옮기기만 했으면)
**다시 시켜라.** `PROMPT.md` 맨 아래에 다시 시킬 때 붙일 문장이 있다.

---

## 4. 텔레그램 봇 설계

### 제일 중요한 원칙 ⚠️

> **QWEN에게 채점이나 발송 로직을 맡기지 마라.**

LLM은 코드 실행 결과를 **상상해서** 답한다. "이 코드 맞나요?" 물으면 그럴듯하게 틀린 판정을 내린다.
발송·기록·판정은 전부 **평범한 파이썬 코드**가 해야 한다.

| 하는 일 | 누가 |
|---|---|
| 문제 카드 고르기 · 보내기 | **코드** (`bot.py`) |
| 답지 보내기 | **코드** |
| 진행 기록 · 다음 문제 정하기 | **코드** |
| 막혔을 때 추가 설명, 질문 답변 | **QWEN** (근거는 카드의 답지 내용) |

QWEN이 **문제를 새로 지어내게 하면 안 된다.** 그러면 정답 보장이 사라진다.
반드시 `cards.json` 에 있는 것만 쓰게 한다.

### 동작 방식 — 플래시카드

목적이 "**정답지를 계속 외우는 것**" 이므로 채점 기능은 필요 없다. 이렇게 하면 된다.

```
1. 정해진 시각에 봇이 카드 3장을 보낸다  (그래프 1장 · 자료구조 1장 · DP 1장)
   → 이때는 "문제" 부분만 보낸다

2. 각 메시지 아래 버튼 두 개
   [답지 보기]   [나중에]

3. [답지 보기] 를 누르면 정답 코드 + 해설을 보낸다

4. 답지 아래 버튼 두 개
   [알겠다]   [또 보여줘]

   [알겠다]   → 이 카드는 간격을 늘린다 (3일 뒤 → 7일 뒤 → 30일 뒤)
   [또 보여줘] → 내일 다시 보낸다
```

이게 **간격 반복(spaced repetition)** 이다. 모르는 카드가 자주 돌아오고 아는 카드는 뜸해진다.

### cards.json 구조

```json
{
 "categories": [
  {
   "key": "02_ds",
   "title": "자료구조 응용",
   "cards": [
    {
     "no": "9012",
     "title": "괄호",
     "tier": "Silver IV",
     "topic": "스택 (카운터로 대체 가능)",
     "category": "02_ds",
     "problem": "## 문제 ... ## 입력 ... ## 출력 ... ## 예제 ...",
     "answer":  "## 풀이 아이디어 ... ## 코드 따라가기 ... ## 외울 것 ...",
     "code":    "정답 파이썬 코드 전문",
     "has_bug_note": false
    }
   ]
  }
 ]
}
```

- **문제 메시지** = `title` + `tier` + `problem`
- **답지 메시지** = `code` 를 ``` 블록으로 + `answer`
- `has_bug_note` 가 `true` 면 그 카드의 정답 코드에 알려진 버그가 있다는 표시다 (답지에 설명이 들어있다)

### 텔레그램 API — 설치할 것 없다

텔레그램 봇 API는 그냥 HTTP다. `pip install` 필요 없이 표준 라이브러리로 된다.

```python
import json, urllib.request

TOKEN = "봇토큰"          # @BotFather 에서 발급
API = "https://api.telegram.org/bot" + TOKEN + "/"

def call(method, **params):
    data = json.dumps(params).encode("utf-8")
    req = urllib.request.Request(API + method, data=data,
                                 headers={"Content-Type": "application/json"})
    return json.load(urllib.request.urlopen(req))

# 메시지 보내기
call("sendMessage", chat_id=내chat_id, text="안녕", parse_mode="Markdown")

# 버튼 달기
call("sendMessage", chat_id=내chat_id, text="답지 볼래?",
     reply_markup={"inline_keyboard": [[
         {"text": "답지 보기", "callback_data": "ans:9012"},
         {"text": "나중에",   "callback_data": "skip:9012"}]]})

# 받은 것 확인 (폴링)
updates = call("getUpdates", offset=마지막본거+1, timeout=30)
```

필요한 것은 **봇 토큰**(@BotFather에서 `/newbot`)과 **내 chat_id**
(봇에게 아무 말이나 보낸 뒤 `getUpdates` 에 찍힌 숫자) 두 개뿐이다.

### 준비물 체크리스트

- [ ] @BotFather 에서 봇 만들고 토큰 받기
- [ ] 봇에게 말 걸어서 chat_id 알아내기
- [ ] 토큰은 코드에 박지 말고 환경변수나 `.env` 로 (이 레포는 **공개**다)
- [ ] `.gitignore` 에 `.env` 추가
- [ ] 진행 기록 저장 파일 (`study/cards/progress.json` 같은 것)

### 봇을 어디서 돌리나

| 방법 | 장점 | 단점 |
|---|---|---|
| 내 PC 에서 `python bot.py` | 제일 간단 | PC 꺼지면 멈춤 |
| GitHub Actions cron | 공짜 · 항상 켜짐 | 최소 5분 간격, 버튼 응답이 느림 |
| 무료 클라우드 (Railway 등) | 항상 켜짐 · 즉시 응답 | 가입 필요 |

**처음엔 내 PC 로 시작해라.** 잘 돌아가는 걸 확인한 뒤에 옮기면 된다.

---

## 5. 안 될 때

### 카드 품질이 나쁘다
→ `PROMPT.md` 맨 아래 "품질이 떨어지면" 참고. 그래도 안 되면 클로드에게 그 카드 하나를 직접 시켜라.

### `build.py` 가 `[X]` 를 뱉는다
→ 빠진 항목 이름을 알려준다. 그 `## 항목` 을 카드에 추가하면 된다.

### 정답 코드가 이상해 보인다
→ 실제로 틀린 게 하나 발견됐다. **`ref/2579.py` 는 계단이 정확히 3개일 때 틀린다**
   (`if n > 3` 때문에 n=3이 `sum(전부)` 로 빠진다. 규칙 위반이라 오답).
   이런 걸 또 찾으면 카드에 `## ⚠️ 이 코드의 버그` 로 적어둔다. 코드 자체는 고치지 않는다.

### 그 외
→ 클로드에게 이 문서를 보여주고 물어보면 된다.

---

## 6. 기억할 것

1. **`ref/` 는 건드리지 않는다.** 검증된 정답 원본이다
2. **지문은 코드에서 복원한다.** 지어내면 정답과 어긋난다
3. **QWEN은 설명만, 판정은 코드가**
4. **카드는 한 번에 한 장씩.** 몰아서 시키면 품질이 무너진다
5. 레포 루트의 `Python/`, `백준/`, `README.md` 는 **BaekjoonHub 확장이 자동 관리**한다. 건드리지 말 것
6. 작업 시작 전 `git pull --rebase`, 끝나면 `git add` → `commit` → `push`
